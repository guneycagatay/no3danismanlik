"use client";

import AppointmentFunnel from "./AppointmentFunnel";

export default function LeadFunnel() {
    return (
        <div className="w-full">
            <AppointmentFunnel onSuccess={() => {}} />
        </div>
    );
}
